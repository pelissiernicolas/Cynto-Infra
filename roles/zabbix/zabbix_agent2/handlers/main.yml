---
- name: Mettre à jour le cache APT
  ansible.builtin.apt:
    update_cache: true
    lock_timeout: 300

- name: Redémarrer Zabbix Agent 2
  ansible.builtin.service:
    name: zabbix-agent2
    state: restarted
